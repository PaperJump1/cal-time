#include <iostream>
#include <iomanip>
#include <iterator>
#include <algorithm>
#include <functional>
#include <string>
#include <ctime>

int main() {
	clock_t start_t,end_t;
	start_t = clock();
	for (int i = 0; i < 99999999; i++) {

	}
	for (int i = 0; i < 99999999; i++) {

	}
	end_t = clock();
	std::cout<<(double)(end_t-start_t)/ CLOCKS_PER_SEC;
}
